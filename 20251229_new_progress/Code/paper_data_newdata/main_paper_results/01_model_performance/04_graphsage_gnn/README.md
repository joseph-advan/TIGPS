# 04 GraphSAGE GNN

## Purpose

Run GraphSAGE as the GNN baseline for W2 -> W2 and W2 -> W3.

## Version Rule

GraphSAGE is run only with drop + decomposition node features and peer nomination graph edges. No no-drop GNN version is run or reported.

## Main Output

- `outputs/graphsage_gnn_performance.xlsx`
- `outputs/GRAPHSAGE_GNN_SUMMARY.md`
